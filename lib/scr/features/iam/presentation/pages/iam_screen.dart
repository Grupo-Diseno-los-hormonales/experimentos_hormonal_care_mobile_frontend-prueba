import 'package:flutter/material.dart';

class IamScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('IAM Screen'),
      ),
      body: Center(
        child: Text('Pantalla de IAM'),
      ),
    );
  }
}
